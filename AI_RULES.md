# XLaunch 开发规则

本文件用于约束所有参与 XLaunch 开发的人员和 AI 助手。若实现方案与本文件冲突，应先停止并取得项目维护者确认。

## 项目定位

XLaunch 是一个纯粹的 Windows 快速启动工具，只负责以分类和图标组织本地启动项，并快速启动它们。

XLaunch 不做：

- 插件系统或插件市场
- Everything 集成、文件索引或搜索引擎
- Flow Launcher 式的搜索框工作流
- AI 功能
- 网络服务、云同步或 WebDAV
- 账号系统
- 数据库（除非未来得到明确批准）

## 固定技术栈

- Visual Studio 2026
- C++20
- Win32 API
- Dear ImGui
- DirectX 11
- nlohmann/json

未经明确确认，不得替换 UI 框架、渲染后端、配置格式或构建体系。

## 固定界面方向

- 顶部使用横向分类栏。
- 主区域使用图标网格。
- 不得改为侧边栏。
- 不得改为资源管理器布局。
- 不得以搜索框作为主界面或主要交互方式。

## 工程原则

- 优先保证启动速度、低内存占用和低空闲 CPU 占用。
- 保持代码直接、清晰、可维护，不为假设中的需求提前抽象。
- 模块只承担一种清晰职责，依赖方向必须明确。
- 优先使用 RAII 管理资源，避免无归属的全局状态。
- Windows 平台细节集中在平台层，不扩散到数据模型和业务层。
- 配置文件应便于人工阅读和编辑，并提供安全的加载失败处理。
- 新增第三方依赖必须先说明必要性、体积和维护成本，并取得确认。
- 不随意删除、移动或大规模重构已有文件。
- 每个阶段只完成已确认的范围，避免一次开发整个项目。

## 变更流程

1. 修改前先阅读相关文件和文档。
2. 说明目标、边界和可能影响。
3. 采用最小可验证变更。
4. 编译并验证与改动风险相称的场景。
5. 更新受影响的文档。
6. 未经确认，不进入路线图的下一阶段。

## 当前阶段限制

当前阶段实现系统托盘常驻、全局键盘/鼠标手势呼出、当前用户开机自启和本地配置备份/导入。仍不得加入图标/分类拖动排序、插件、搜索、Everything、网络、WebDAV 或动画；进入下一阶段前必须取得维护者确认。
